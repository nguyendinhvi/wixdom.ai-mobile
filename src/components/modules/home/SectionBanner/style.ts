import { StyleSheet } from 'react-native';

export const sectionBannerStyle = StyleSheet.create({
  container: {
    justifyContent: 'center',
    paddingHorizontal: 20,
    marginBottom: 70,
  },
});
